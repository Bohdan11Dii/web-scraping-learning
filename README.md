# web-scraping-learning

readme_content = """# Project: Quotes Scraper (Парсер цитат)

Цей проєкт є частиною вивчення веб-скрапінгу на Python. Він демонструє базові навички роботи з мережевими запитами,
розбором HTML-структури сторінок та збереженням зібраних даних у структурований формат.

Проєкт побудовано з дотриманням принципу **SOLID** (Single Responsibility Principle — кожна функція відповідає лише за
одну задачу).

---

## 📋 Поставлені та виконані задачі

У межах цього завдання було успішно реалізовано наступний функціонал:

* **[x] Розділення обов'язків архітектури коду (SOLID):**
    * Створено окремий мережевий рівень («Кур'єр»), який не займається аналізом тексту.
    * Створено ізольований рівень бізнес-логіки («Аналітик»), який працює лише з готовим текстом HTML.
* **[x] Безпечна робота з мережею:**
    * Реалізовано обробку статус-кодів відповідей сервера (перевірка на `200 OK`).
    * Інтегровано обов'язковий контроль таймаутів (`timeout=10`), щоб уникнути зависання програми.
    * Додано перехоплення мережевих винятків за допомогою `requests.RequestException`.
* **[x] Скрапінг структурованих даних:**
    * Реалізовано пошук усіх карток цитат на сторінці через клас `.quote`.
    * Налаштовано точковий збір: текст цитати (`span.text`), автор (`small.author`) та динамічний список усіх пов'язаних
      тегів (`a.tag`).
* **[x] Автоматичний обхід пагінації (Багатосторінковий парсинг):**
    * Реалізовано динамічний пошук посилання на наступну сторінку через елемент моніторингу кнопки `.next a`.
    * Налаштовано циклічний алгоритм, який автоматично збирає дані з усіх 10 сторінок сайту, поки кнопка "Next" не
      зникне.
    * Додано правильне склеювання базового домену з відносними шляхами (наприклад, `/page/2/`).
* **[x] Експорт та збереження результатів:**
    * Написано утиліту збереження даних у формат `JSON`.
    * Забезпечено коректне відображення кирилиці та спецсимволів завдяки параметрам `encoding='utf-8'` та
      `ensure_ascii=False`.
    * Налаштовано гарне форматування файлу (`indent=4`).

---

## 🛠️ Стек технологій

* **Мова:** Python 3.x
* **HTTP-запити:** `requests`
* **Парсинг HTML:** `BeautifulSoup4` (html.parser)
* **Формат збереження:** `json`

---------------------------------------------------------------------------------

# 2. Parse book site

## 🚀 Твій перший крок завдання про Книги:

Давай почнемо з малого, як ти і хотів.

Крок №1:

1. Створи функцію fetch_html(url, headers) з обробкою винятків (try/except), яка буде приймати URL та заголовки й
   повертати HTML-код сторінки.
2. Створи словник headers з реальним User-Agent (можеш скопіювати мій приклад вище).
3. Виклич цю функцію для першої сторінки магазину: http://books.toscrape.com/ і просто виведи в консоль статус-код
   відповіді (response.status_code), щоб переконатися, що запит успішний (код 200).

## 🚀 Крок №2: Аналіз картки книги та створення парсера однієї сторінки

Тепер, коли мережевий рівень працює стабільно, переходимо до бізнес-логіки (Аналітика).

Твоє завдання на цей крок:

Імпортуй BeautifulSoup з bs4.

Створи функцію parse_page_books(html_content), яка прийматиме сирий HTML однієї сторінки.

Тобі потрібно знайти на сторінці всі блоки з книгами. (Підказка: кожна книга на цьому сайті обгорнута в тег <article> з
класом product_pod).

Всередині цього блоку знайди та витягни:

Назву книги (вона лежить у тегу h3, всередині якого є посилання a. Зверни увагу: повна назва книги часто записана в
атрибуті title цього посилання, бо видимий текст може бути обрізаний трьома крапками!).

Ціну (тег p з класом price_color).

Наявність (тег p з класом instock availability). Очисть текст від зайвих пробілів та переносів рядків за допомогою
.strip().

Посилання на картинку (тег img з класом thumbnail, тобі потрібен його атрибут src).